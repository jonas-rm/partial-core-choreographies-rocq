% This is samplepaper.tex, a sample chapter demonstrating the
% LLNCS macro package for Springer Computer Science proceedings;
% Version 2.21 of 2022/01/12

\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{todonotes}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{jolie}
\usepackage{lstcoq}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{placeins}

% \lstdefinestyle{haskell}{
%   frame=none,
%   xleftmargin=2pt,
%   stepnumber=1,
%   numbers=left,
%   numbersep=5pt,
%   numberstyle=\ttfamily\tiny\color[gray]{0.3},
%   belowcaptionskip=\bigskipamount,
%   captionpos=b,
%   escapeinside={*'}{'*},
%   language=haskell,
%   tabsize=2,
%   emphstyle={\bf},
%   commentstyle=\it,
%   stringstyle=\mdseries\rmfamily,
%   showspaces=false,
%   keywordstyle=\bfseries\rmfamily,
%   columns=flexible,
%   basicstyle=\small\sffamily,
%   showstringspaces=false,
%   morecomment=[l]\%,
% }

\newlang{coq}{Coq}{syellow}
\newlang{haskell}{Haskell}{sgreen}
\newcommand{\hc}{\lstinline[language=Haskell,style=solarized-light]}
\newlang{jolie}{Jolie}{smagenta}
\newcommand{\jc}{\lstinline[language=Jolie,style=solarized-light]}

\newcommand{\dollar}{\mbox{\textdollar}}
\newcommand{\proc}[1]{\texttt{#1}}
\newcommand{\toolname}{\texttt{hacc}}

\begin{document}

% \title{Now it runs!}
% \subtitle{Certified Compilation of Choreographies with \toolname{}}

\title{Certified Compilation of Choreographies with \toolname{}}

% If the paper title is too long for the running head, you can set
% an abbreviated paper title here
%\titlerunning{Abbreviated paper title}

\author{
  Luís Cruz-Filipe \orcidID{0000-0002-7866-7484} \and
  Lovro Lugović \orcidID{0000-0001-9684-9567} \and
  Fabrizio Montesi \orcidID{0000-0003-4666-901X}
}

% First names are abbreviated in the running head.
% If there are more than two authors, 'et al.' is used.
\authorrunning{L. Cruz-Filipe et al.}

\institute{
  Department of Mathematics and Computer Science, University of Southern Denmark
  \email{\{lcf, lugovic, fmontesi\}@imada.sdu.dk}
}

\maketitle

\begin{abstract}
  Programming communicating processes is challenging, because it requires writing separate programs that perform compatible send and receive actions at the right time during execution.
  Leaving this task to the programmer can easily lead to bugs.

  \emph{Choreographic programming} is a paradigm that addresses this challenge by equipping developers with high-level abstractions for codifying the desired structures of communications among processes from a global viewpoint.
  Given a choreographic program (called \emph{choreography}), implementations of the involved processes can be automatically compiled by a process known as \emph{endpoint projection (EPP)}.

  While choreographic programming removes the possibility of manual mistakes in the implementation of communications with local send and receive actions, the correctness of a choreographic programming framework crucially hinges on the correctness of its compiler.
  Since choreography compilation is technically involved, previous efforts developed mechanisations of theories of choreographic programming and proved them correct in theorem provers.
  In this paper, we build on one of these formalisations to construct a toolchain that produces executable code from a choreography.

  \keywords{Choreographic programming \and Certified compilation \and Jolie \and Formal verification}
\end{abstract}

\section{Introduction}

In traditional distributed programming, the programmer is tasked with writing the implementation of each \emph{process} (endpoint) as a separate program, taking care of correctly matching send and receive actions in the different programs.
This approach is known to be cumbersome and error-prone \cite{LLLG16}.

\emph{Choreographic programming} \cite{M13p} is an alternative paradigm for distributed programming, where developers are equipped with high-level abstractions to specify the desired communications between processes from a global viewpoint.
Given a choreographic program (called \emph{choreography}), correct implementations of all of the involved processes can then be automatically generated by means of a compilation procedure known as \emph{endpoint projection (EPP)}~\cite{M23}.
This removes the possibility for manual mistakes in the programming of communication actions and provides important theoretical advantages, like deadlock-freedom by design -- the distributed code generated from a choreography is always deadlock-free, since it is syntactically impossible to write unmatched communications in a choreographic programming language~\cite{CM13}.
In addition to these correctness advantages, this also saves time and lets the programmer focus on the bigger picture of the protocol being developed.

Defining and implementing EPP is technically involved~\cite{SY19}, which motivated substantial efforts in the mechanisation of theories of choreographic programming using interactive theorem provers~\cite{CMP21,CMP21b,PGSN22,HG22}.
Among these, the formalisation of the choreographic programming language \emph{Core Choreographies (CC)} and its EPP to the process calculus of \emph{Stateful Processes (SP)} was designed with broad applicability in mind~\cite{CMP22}.
Specifically, its design allows for annotating communications with arbitrary metadata and is parametric on the languages used to express local computation, data, process identifiers, etc.

In this work, for the first time, we reap the benefits of~\cite{CMP22} to develop \toolname{} (pronounced ``hack''): a tool for compiling choreographies in CC to executable code.
As target language for this executable code, we use the service-oriented programming language Jolie~\cite{MGZ14}.
Additionally, \toolname{} is designed with future extensibility in mind, so that new target languages can be added.

\begin{figure}[h]
  \centering
  \includegraphics{arch.drawio.pdf}
  \caption{The architecture of \toolname{}'s compilation pipeline.}
  \label{fig:arch}
\end{figure}

More technically, \toolname{}'s architecture consists of two compilation phases (\cref{fig:arch}).
The first phase (projection) uses EPP to translate a choreography into an abstract representation of process programs given in SP, which we use as an intermediate representation (IR).
This phase is certified: it is implemented by Haskell code that we extracted from the Coq formalisation of EPP in~\cite{CMP21b}.
The second phase (code generation) takes care of translating the abstract send and receive actions in the IR into code in an executable programming language.
\todo{Not only send and receive actions, since we also have conditionals?}
In our current implementation for Jolie, this phase is not formally verified.
Nonetheless, it is a homeomorphic transformation that follows the term structure of the IR in a straightforward mechanical fashion.

Our development thus allows for coding and executing choreographies given in a well-understood language (CC)~\cite{CM20,CMP21,CMP21b,M23}.
Also, it confirms the informal claim made in~\cite{CMP22} that the formalisation of CC was made with flexibility in mind for this kind of applications.
In particular, our application to Jolie did not require any modification to the formalisation, but just an appropriate instantiation of its parameters and a simple interfacing of its extracted data types with the other components of our compilation pipeline in Haskell.

\paragraph*{Structure.}

\Cref{sec:arch} gives an overview of our compiler's architecture and describes its implementation.
\Cref{sec:comp} explains how we map the process language to Jolie in order to generate executable Jolie code consisting of multiple independent services.

\paragraph*{Related work.}

There are two other formalisations of choreographic programming in the literature.

Kalas~\cite{PGSN22} is a choreographic programming language formalised in the Hol4 proof assistant.
It comes with an end-to-end certified compiler that targets CakeML~\cite{MO12}, a formally-verified subset of the ML language.
By contrast, we take a technology-agnostic approach that allows for reusing our compiler infrastructure for different target languages.

Pirouette~\cite{HG22} is a functional choreographic programming language formalised in Coq, which similarly to~\cite{CMP21} can be instantiated with different languages for local computation.
However, it has not yet been used to implement a choreographic compiler that targets executable code.

\section{Choreographies in \toolname{}}
\label{sec:arch}

\paragraph*{Representation.}

We represent choreographies and processes (terms of CC and SP) with Haskell data types that have been automatically extracted from the Coq formalisation.
However, a few considerations are necessary given that Coq is a dependently-typed language, while all of the languages it can extract to are not.
As mentioned before, the formalisation is parametric over the types used to represent process identifiers, (recursion) variables, terms of the local computation language, etc.
Because this is done using the dependently-typed features of Coq, certain ``artefacts'' appear in the code extracted from Coq into Haskell.

Particularly, instead of using Haskell's type system and parametric polymorphism, the extracted code uses Haskell's \hc{Any} type to achieve genericity.
The \hc{unsafeCoerce} function has to be used when interfacing with the extracted code in order to coerce them to and from \hc{Any}.
To deal with the verbosity we provide a more ergonomic interface with a thin \textit{wrapper} (\cref{fig:arch}) around the extracted code.
The wrapper hides the necessary coercions and models the terms using Haskell's parametric polymorphism.
Again, this part is not formally verified, but as before, the wrapper follows the term structure of CC and SP so checking it for correctness manually is not a problem.
\todo{Use something other than ``wrapper''?}

Our final interface is shown in \cref{fig:iface}.
We fix the types of process identifiers (\hc{Pid}), variable names (\hc{Var}, \hc{RecVar}), selection labels (\hc{Label}) and annotations (\hc{Ann}) for simplicity's sake.
The \hc{Label} type is a binary sum type with \hc{CLeft} and \hc{CRight} as its constructors, while others are just wrappers around \hc{String}s and are used as identifiers.

A choreography is represented with the type \hc{Choreography e b} which is parametric on the terms of the local computation and boolean expression languages (\hc{e} and \hc{b}).
Processes can perform point-to-point interactions (\hc{Interaction})---whether plain communications (\hc{Com}) that transfer values of expressions to remote variables, or selections (\hc{Sel}) that transfer labels.
Choreographies can also include conditionals (\hc{CCond}) based on boolean expressions and invoke recursive procedures (\hc{CCall}).
\hc{CProgram} models the overall choreographic program which carries the definitions of any recursive procedures (\hc{CDefSet}) in addition to the main choreography.
\todo{Mention the elision of runtime terms?}

A process' projected behaviour is expressed with the type \hc{Behaviour e b}, again parametric on the local languages.
\hc{Send} and \hc{Recv} correspond to the respective communication actions, while \hc{BCond} and \hc{BCall} are as in CC.
The remaining terms \hc{Choose} and \hc{Offer} are used to implement selections, where one process can \textit{choose} which of the \textit{offered} behaviours another process should execute.
Processes are named by identifiers and grouped into a \hc{Network}, which is then paired with the projections of recursive procedures for each process (\hc{BDefSet}) to form a \hc{BProgram}.

Finally, \hc{epp} performs the projection of a \hc{CProgram} to a \hc{BProgram} using the extracted EPP as its foundation.
We use the \hc{Maybe} type to handle the case when a choreography is not projectable.
\todo{Move processes and \hc{epp} to section 3?}

\begin{figure}[h]
  \centering
  \begin{haskelllisting}
data Eta e
  = Com Pid e Pid Var
  | Sel Pid Pid Label

data Choreography e b
  = CEnd
  | Interaction (Eta e) Ann (Choreography e b)
  | CCond Pid b (Choreography e b) (Choreography e b)
  | CCall RecVar

newtype CDefSet e b = CDefSet [(RecVar, Choreography e b)]
newtype CProgram e b = CProgram (CDefSet e b, Choreography e b)

data Behaviour e b
  = BEnd
  | Send Pid e Ann (Behaviour e b)
  | Recv Pid Var Ann (Behaviour e b)
  | Choose Pid Label Ann (Behaviour e b)
  | Offer Pid (Maybe (Ann, Behaviour e b)) (Maybe (Ann, Behaviour e b))
  | BCond b (Behaviour e b) (Behaviour e b)
  | BCall (RecVar, Pid)

newtype BDefSet e b = BDefSet [((RecVar, Pid), Behaviour e b)]
newtype Network e b = Network [(Pid, Behaviour e b)]
newtype BProgram e b = BProgram (BDefSet e b, Network e b)

epp :: CProgram e b -> Maybe (BProgram e b)
  \end{haskelllisting}
  \caption{Representation of choreographies and processes in \toolname{}.}
  \label{fig:iface}
\end{figure}

\paragraph*{Distributed authentication.}

\Cref{fig:ex-chor} shows the \todo{``the''---I assume this is a well-known example by now} distributed authentication choreography encoded as a \hc{CProgram}.

Instead of working with our data types directly we define a convenient set of combinators for building choreographies.
The \hc{prog} combinator creates a \hc{CProgram} given a pair of recursive procedure definitions and the main choreography.
A choreography is given as a list of instructions---communications, selections, conditionals and calls---all built using the corresponding combinators---\hc{com}, \hc{sel}, \hc{cond} and \hc{call}---which the builder strings together to produce a CC term in our representation.
\hc{ann} can be used to annotate any of the communication or selection instructions.
We do not make use of recursive procedures in order to keep the examples short, but they are straightforward to use and amount to ``named choreographies''.

The choreography is conceptually simple: \proc{Client} wishes to authenticate with \proc{Ip} (identity provider) in order to receive a token from \proc{Server}.
In the beginning, \proc{Client} sends its credentials to \proc{Ip}.
After checking the credentials for validity, \proc{Ip} decides which branch of the conditional to take and communicates its decision to \proc{Server}.
\proc{Server} receives the decision and, if the authentication was successful, sends a token to \proc{Client}.

Two things are of note.
First, we annotate all of the interactions with strings that carry the names of the operation that is being performed.
This will be useful later during code generation and allows the user to have some control over the produced executable code and its interface.
Second, we model the terms of the local computation and boolean expression languages as plain strings for simplicity.
The strings will be interpolated as-is into the generated executable code.

\begin{figure}[h]
  \centering
%   \begin{haskelllisting}[]
% CProgram
%   (CDefSet [],
%    Interaction (Com (Pid "Client") "credentials" (Pid "Ip") (Var "credentials")) (Ann "authenticate")
%      (CCond (Pid "Ip") "check(credentials)"
%        (Interaction (Sel (Pid "Ip") (Pid "Server") CLeft) (Ann "authOk")
%          (Interaction (Sel (Pid "Ip") (Pid "Client") CLeft) (Ann "authOk")
%            (Interaction (Com (Pid "Server") "makeToken" (Pid "Client") (Var "token")) (Ann "acceptToken")
%              CEnd)))
%        (Interaction (Sel (Pid "Ip") (Pid "Server") CRight) (Ann "authFail")
%          (Interaction (Sel (Pid "Ip") (Pid "Client") CRight) (Ann "authFail") CEnd))))
%   \end{haskelllisting}
  \begin{haskelllisting}[]
auth :: CProgram String String
auth = prog ([],
  [ann "authenticate" $\dollar$ com c "credentials" ip credentials,
   cond ip "check(credentials)"
     ([ann "authOk" $\dollar$ left ip s,
       ann "authOk" $\dollar$ left ip c,
       ann "acceptToken" $\dollar$ com s "makeToken" c token],
      [ann "authFail" $\dollar$ right ip s,
       ann "authFail" $\dollar$ right ip c])])
  where
    [ip, s, c] = pids ["Ip", "Server", "Client"]
    [credentials, token] = vars ["credentials", "token"]
  \end{haskelllisting}
%   \begin{haskelllisting}[]
% auth :: CProgram String String
% auth = prog
%   ([]
%   , [ ann "authenticate" $\dollar$ com c "credentials" ip credentials
%     , cond ip "check(credentials)"
%       ( [ ann "authOk" $\dollar$ left ip s
%         , ann "authOk" $\dollar$ left ip c
%         , ann "acceptToken" $\dollar$ com s "makeToken" c token ]
%       , [ ann "authFail" $\dollar$ right ip s
%         , ann "authFail" $\dollar$ right ip c ] ) ] )
%   where
%     [ip, s, c] = pids ["Ip", "Server", "Client"]
%     [credentials, token] = vars ["credentials", "token"]
%   \end{haskelllisting}
  \caption{The distributed authentication choreography in CC.}
  \label{fig:ex-chor}
\end{figure}

\section{Compilation to Jolie}
\label{sec:comp}

\paragraph*{Projection.}

Projecting the distributed authentication choreography from \cref{fig:ex-chor} gives us the process IR seen in \cref{fig:ex-comp}.
We show only the behaviour for \proc{Client} to save on space.
Note how the annotations from the choreography have been preserved and propagated to the projection.

\paragraph*{Code generation.}

The process IR can now be compiled down to executable code.
We use the service-oriented language Jolie to generate a \jc{service} for each process.

% Annotations serve a special purpose and will be explained in \cref{sec:comp}.
% we use Coq to \textit{extract} a formally verified implementation of the EPP and use it as the core of our compiler.
% With the critical part of the compiler taken care of automatically, we can focus just on mapping the necessary process semantics to our executable language.
% We use Jolie as our final compilation target, producing for each process a Jolie service that implements its part of the overall choreographic behaviour and can be run independently.

% \begin{figure}[h]
%   \centering
% %   \begin{haskelllisting}[]
% % BProgram
% %  (BDefSet [],
% %   Network [
% %     ...,
% %     (Pid "Client",
% %      Send (Pid "Ip") "credentials" (Ann "authenticate")
% %        (Offer (Pid "Ip")
% %          (Just (Ann "authOk",
% %                 Recv (Pid "Server") (Var "token") (Ann "acceptToken") BEnd))
% %          (Just (Ann "authFail", BEnd))))])
% %   \end{haskelllisting}
% %   \begin{haskelllisting}[]
% % BProgram
% %  (BDefSet [],
% %   Network [
% %     (Pid "Ip",
% %      Recv (Pid "Client") (Var "credentials") (Ann "authenticate")
% %        (BCond "check(credentials)"
% %          (Choose (Pid "Server") CLeft (Ann "authOk")
% %            (Choose (Pid "Client") CLeft (Ann "authOk") BEnd))
% %          (Choose (Pid "Server") CRight (Ann "authFail")
% %            (Choose (Pid "Client") CRight (Ann "authFail") BEnd)))),
% %     (Pid "Server",
% %      Offer (Pid "Ip")
% %        (Just (Ann "authOk",
% %               Send (Pid "Client") "makeToken" (Ann "acceptToken") BEnd))
% %        (Just (Ann "authFail", BEnd))),
% %     (Pid "Client",
% %      Send (Pid "Ip") "credentials" (Ann "authenticate")
% %        (Offer (Pid "Ip")
% %          (Just (Ann "authOk",
% %                 Recv (Pid "Server") (Var "token") (Ann "acceptToken") BEnd))
% %          (Just (Ann "authFail", BEnd))))])
% %   \end{haskelllisting}
% %   \begin{haskelllisting}[]
% % BProgram
% %  ( BDefSet []
% %  , Network
% %    [ ( Pid "Ip"
% %      , Recv (Pid "Client") (Var "credentials") (Ann "authenticate")
% %          (BCond "check(credentials)"
% %            (Choose (Pid "Server") CLeft (Ann "authOk")
% %              (Choose (Pid "Client") CLeft (Ann "authOk") BEnd))
% %            (Choose (Pid "Server") CRight (Ann "authFail")
% %              (Choose (Pid "Client") CRight (Ann "authFail") BEnd))))
% %    , ( Pid "Server"
% %      , Offer (Pid "Ip")
% %          (Just ( Ann "authOk"
% %                , Send (Pid "Client") "makeToken" (Ann "acceptToken")
% %                  BEnd))
% %          (Just ( Ann "authFail"
% %                , BEnd ) ) )
% %    , ( Pid "Client"
% %      , Send (Pid "Ip") "credentials" (Ann "authenticate")
% %         ( Offer (Pid "Ip")
% %           ( Just ( Ann "authOk"
% %                  , Recv (Pid "Server") (Var "token") (Ann "acceptToken") BEnd))
% %           ( Just ( Ann "authFail"
% %                  , BEnd ) ) ) ) ] )
% %   \end{haskelllisting}
%   \caption{The projection of the choreography for \proc{Client} in SP.}
%   \label{fig:ex-comp}
% \end{figure}

\begin{figure}[h]
  \centering
  \begin{haskelllisting}[]
BProgram (BDefSet [], Network [...,
  (Pid "Client",
   Send (Pid "Ip") "credentials" (Ann "authenticate")
     (Offer (Pid "Ip")
       (Just (Ann "authOk",
              Recv (Pid "Server") (Var "token") (Ann "acceptToken") BEnd))
       (Just (Ann "authFail", BEnd))))])
  \end{haskelllisting}
  \begin{jolielisting}
service Client {
  main {
    authenticate@Ip( credentials )
    [ authOk() ] {
      acceptToken( token ) } } }

service Ip {
  main {
    authenticate( credentials )
    if ( check@Util( credentials ) ) {
      authOk@Server()
      authOk@Client()
    } else {
      authFail@Server()
      authFail@Client() } } }

service Server {
  main {
    [ authOk() ] {
      acceptToken@Client( makeToken@Util() ) } } }
  \end{jolielisting}
  \caption{The results of projection and code generation.}
  \label{fig:ex-comp}
\end{figure}

\FloatBarrier

% \section{Discussion and Conclusion}
% \label{sec:disc}

% \todo{Compare to Kalas: technology-agnostic process IR with flexible annotations.}
% \todo{Future work: normal surface syntax instead of raw CC terms.}

% \subsubsection{Acknowledgements}

% Please place your acknowledgments at the end of the paper, preceded by an unnumbered run-in heading (i.e. 3rd-level heading). \todo{Acknowledgements?}

% ---- Bibliography ----
%
% BibTeX users should specify bibliography style 'splncs04'.
% References will then be sorted and formatted in the correct style.

\bibliographystyle{splncs04}
\bibliography{main}

\end{document}
