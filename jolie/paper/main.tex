% This is samplepaper.tex, a sample chapter demonstrating the
% LLNCS macro package for Springer Computer Science proceedings;
% Version 2.21 of 2022/01/12

\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{todonotes}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{jolie}
\usepackage{lstcoq}
\usepackage{cleveref}

% \lstdefinestyle{haskell}{
%   frame=none,
%   xleftmargin=2pt,
%   stepnumber=1,
%   numbers=left,
%   numbersep=5pt,
%   numberstyle=\ttfamily\tiny\color[gray]{0.3},
%   belowcaptionskip=\bigskipamount,
%   captionpos=b,
%   escapeinside={*'}{'*},
%   language=haskell,
%   tabsize=2,
%   emphstyle={\bf},
%   commentstyle=\it,
%   stringstyle=\mdseries\rmfamily,
%   showspaces=false,
%   keywordstyle=\bfseries\rmfamily,
%   columns=flexible,
%   basicstyle=\small\sffamily,
%   showstringspaces=false,
%   morecomment=[l]\%,
% }

\newlang{coq}{Coq}{syellow}
\newlang{haskell}{Haskell}{sgreen}
\newcommand{\hc}{\lstinline[language=Haskell,style=solarized-light]}
\newlang{jolie}{Jolie}{smagenta}

\begin{document}

\title{\texttt{cc2jolie}: Certified Compilation of Choreographies to Jolie}

% If the paper title is too long for the running head, you can set
% an abbreviated paper title here
%\titlerunning{Abbreviated paper title}

\author{
  Luís Cruz-Filipe \orcidID{0000-0002-7866-7484} \and
  Lovro Lugović \orcidID{0000-0001-9684-9567} \and
  Fabrizio Montesi \orcidID{0000-0003-4666-901X}
}

% First names are abbreviated in the running head.
% If there are more than two authors, 'et al.' is used.
\authorrunning{L. Cruz-Filipe et al.}

\institute{
  Department of Mathematics and Computer Science, University of Southern Denmark
  \email{\{lcf, lugovic, fmontesi\}@imada.sdu.dk}
}

\maketitle

\begin{abstract}
  Programming communicating processes is challenging, because it requires writing separate programs that perform compatible send and receive actions at the right time during execution.
  Leaving this task to the programmer can easily lead to bugs.
  \emph{Choreographic programming} is an emerging paradigm that seeks to address this challenge, by equipping developers with high-level abstractions for codifying the desired structures of communications among processes from a global viewpoint.
  Given a choreographic program (called \emph{choreography}), implementations of the involved processes can be automatically compiled by a process known as \emph{endpoint projection (EPP)}.
  While choreographic programming removes the possibility of manual mistakes in the implementation of communications with local send and receive actions, the correctness of a choreographic programming framework crucially hinges on the correctness of its compiler.
  Since choreography compilation is technically involved, previous efforts developed mechanisations of theories of choreographic programming and proved them correct in theorem provers.
  In this paper, we build on one of these formalisations to construct a toolchain that produces executable code from a choreography.

  \keywords{Choreographic programming \and Certified compilation \and Jolie \and Formal verification}
\end{abstract}

\section{Introduction}

\emph{Choreographic programming} is a paradigm whose goal is to help programmers write correct distributed programs by using high-level abstractions to specify interactions between communicating processes.
Traditionally, the programmer is tasked with writing the implementation of each \emph{process (endpoint)} separately, having to take great care in matching all of the necessary send--receive communications.
Such an approach has been demonstrated to be cumbersome and error-prone \cite{LLLG16}, which is why choreographic programming instead seeks to solve the problem from a global viewpoint.
A \emph{choreography} is a specification of the overall behaviour of a distributed system, from which implementations of all of the involved endpoints can be generated automatically through a process known as \emph{endpoint projection (EPP)}.
Because a choreography makes all of the communications syntatically manifest, the programmer retains the control over the precise interactions in the choreography.
Furthermore, since it is syntactically impossible to write unmatched communications, we also get the benefit of deadlock-freedom by design.
This saves time and lets the programmer focus on the bigger picture of the protocol being developed.

The described approach is only as correct as the choreographic compiler however, and it has been discovered before that some of the manual proofs of a choreographic compiler's EPP's correctness were not correct and needed fixes.
In order to avoid such problems in the future and have a correctness guarantee, there has been interest in formally verifying the correctness of choreography compilation using theorem provers.
\todo{cite}

In this work, basing our approach on a previous effort of formalizing the compilation of \textit{Core Choreographies (CC)} to \textit{Stateful Processes (SP)} in the Coq theorem prover, we develop \texttt{cc2jolie}, a compiler producing executable Jolie \cite{MGZ14} code.
Specifically, we use Coq to \textit{extract} a formally verified implementation of the EPP and use it as the core of our compiler.
With the critical part of the compiler taken care of automatically, we can focus just on mapping the necessary process semantics to our executable language.
We use Jolie as our final compilation target, producing for each process a Jolie service that implements its part of the overall choreographic behaviour and can be run independently.

\paragraph*{Structure and Related Work.}

\Cref{sec:arch} gives an overview of our compiler's architecture and describes its implementation.
\Cref{sec:jolie} explains how we map the process language to Jolie in order to generate executable Jolie code consisting of multiple independent services.
Finally, we discuss our approach and conclude in \Cref{sec:disc}.

There have been other similar works in certified compilation of choreographies.
Kalas \todo{cite} is a choreographic programming language that is end-to-end certified using the Hol4 proof assistant and compiles the choreography to CakeML, a formally verified subset of the ML language.
Unlike Kalas, we take a technology-agnostic approach that allows us to plug multiple different backends into our compiler's pipeline \todo{Move this to architecture or the discussion?}.
Pirouette \todo{cite} is a language for typed higher-order functional choreographic programming and comes with a formalization in Coq, but it hasn't yet been used to produce a certified implementation of a choreographic compiler.

\section{Architecture and Implementation}
\label{sec:arch}

\paragraph*{Architecture.}

We implemented \texttt{cc2jolie} in Haskell and took a two-phased approach in the design of its compilation pipeline, shown in \Cref{fig:arch}.

The first phase is the \textit{projection}: it takes as input a choreographic program given as a CC term and projects it to a collection of SP terms representing the necessary behaviours of the endpoints.
We use SP as an intermediate representation (IR) of processes.
This part of the compiler uses the formally verified projection procedure that has been automatically extracted from the Coq formalization into Haskell code.
Making use of the extracted code is relatively straightforward, although a few considerations are necessary given that Coq is a dependently-typed language, while all of the languages it can extract to are not.

The second phase of our compiler is the \textit{code generation} which compiles the generated process IR to an executable language.
In this work we focus on generating Jolie code.
However, because of our modular two-phase approach, additional backends could be implemented without having to touch the rest of the compiler.
This phase of the compiler is not formally verified, but it's correctness is much easier to establish than the correctness of projection itself since the code generation proceeds homeomorphically \todo{``proceeds homeomorphically'', is this right?} on the structure of the process IR.

\begin{figure}[h]
  \centering
  \includegraphics{arch.drawio.pdf}
  \caption{The architecture of \texttt{cc2jolie}'s compilation pipeline.}
  \label{fig:arch}
\end{figure}

\paragraph*{Implementation.}

The Coq formalization we use is parametric over the types used to represent process identifiers, variables, terms of the underlying expression language, terms, etc.
Because this is done by using the dependently-typed features of Coq and manipulating types as normal values, certain ``artefacts'' appear in the code extracted from Coq into Haskell.
In particular, instead of using Haskell's type system and parametric polymorphism, the extracted code makes use of GHC's \todo{cite maybe} \hc{Any} type and the \hc{unsafeCoerce} function to perform casts between the provided concrete types and \hc{Any} which the extracted code works on.
While a little clunky to work with, this is not an issue since the only requirement the formalization imposes on the type parameters is that their equality be decidable.
Practically, this means that we just have to be able to provide a function that can determine whether two values of such a type are equal.
Other than that the extracted code treats all of the type parameters as completely opaque.
For this purpose we wrote a thin wrapper over the extracted code that provides a much more pleasant and idiomatic API to work with, hiding the necessary casts and using Haskell's parametric polymorphism.
Again, this part is not formally verified, but just as before, the wrapper follows the structure of the CC and SP terms so checking it for correctness manually is easy.

Our reusable projection phase has the following Haskell interface.
We fix the types of process identifiers (\hc{Pid}), variable names (\hc{Var}, \hc{RecVar}), selection labels (\hc{Label}) and annotations (\hc{Ann}) for simplicity's sake.
Annotations serve a special purpose and will be explained shortly.
All of the mentioned types are just wrappers around \hc{String}s and only serve the purpose of being an identifier that can be compared for equality.
The parametricity that is re-exposed to the user are the types used to represent terms of the expression and boolean expression languages.

\begin{haskelllisting}
data Eta e
  = Com Pid e Pid Var
  | Sel Pid Pid Label

data Choreography e b
  = CEnd
  | Interaction (Eta e) Ann (Choreography e b)
  | CCond Pid b (Choreography e b) (Choreography e b)
  | CCall RecVar
  | RtCall RecVar [Pid] (Choreography e b)

newtype CDefSet e b = CDefSet [(RecVar, Choreography e b)]
newtype CProgram e b = CProgram (CDefSet e b, Choreography e b)

data Behaviour e b
  = BEnd
  | Send Pid e Ann (Behaviour e b)
  | Recv Pid Var Ann (Behaviour e b)
  | Choose Pid Label Ann (Behaviour e b)
  | Offer Pid (Maybe (Ann, (Behaviour e b))) (Maybe (Ann, (Behaviour e b)))
  | BCond b (Behaviour e b) (Behaviour e b)
  | BCall (RecVar, Pid)

newtype BDefSet e b = BDefSet [((RecVar, Pid), Behaviour e b)]
newtype Network e b = Network [(Pid, Behaviour e b)]
newtype BProgram e b = BProgram (BDefSet e b, Network e b)

epp :: CProgram e b -> Maybe (BProgram e b)
\end{haskelllisting}

\section{Compiling Choreographies to Jolie}
\label{sec:jolie}

\begin{jolielisting}
service Client {
  embed Util as Util

  inputPort Input {
    location: "socket://localhost:8080"
    protocol: http { format = "json" }
    interfaces: ClientApi
  }

  outputPort Ip {
    location: "socket://localhost:8081"
    protocol: http { format = "json" }
    interfaces: IpApi
  }

  define X_Client {
    comClient@Ip( credentials )
    [ leftIp() ] {
      comServer( token )
      println@Util( token )
      X_Client
    }
    [ rightIp() ] {
      X_Client
    }
  }

  main {
    X_Client
  }
}
\end{jolielisting}

\section{Discussion and Conclusion}
\label{sec:disc}

\todo{Compare to Kalas: technology-agnostic process IR with flexible annotations.}
\todo{Future work: normal surface syntax instead of raw CC terms.}

\subsubsection{Acknowledgements}

Please place your acknowledgments at the end of the paper, preceded by an unnumbered run-in heading (i.e. 3rd-level heading). \todo{Acknowledgements?}

% ---- Bibliography ----
%
% BibTeX users should specify bibliography style 'splncs04'.
% References will then be sorted and formatted in the correct style.

\bibliographystyle{splncs04}
\bibliography{main}

\end{document}
